<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- GameActivity transitively extends AppCompatActivity, which requires an
         AppCompat theme. Inherit the fullscreen / no-action-bar look. -->
    <style name="Theme.Rustyboi" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>
