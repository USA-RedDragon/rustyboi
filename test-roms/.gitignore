# Built ROMs + intermediate object files (regenerated by `make -C test-roms`).
# keep this unslashed: agent worktrees symlink build to a prebuilt checkout, and
# a trailing slash matches only a real directory, leaving the symlink untracked.
build
build-bench
*.o
